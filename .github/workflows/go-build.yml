name: "go: build"

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
      - name: Build main.go
        run: "go build ./cmd/banshee/main.go"
